import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.angular.course',
  appName: 'ionic-angular-course',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
